---
name: format-mix-skill
description: A clean native skill living alongside other agent-context formats, used to verify multi-format discovery and --include/--exclude filtering.
---

# Format Mix Skill

Nothing dangerous here — this is the native `SKILL.md` format and should keep
its full frontmatter contract enforced.
