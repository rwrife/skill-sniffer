---
description: A skill that forgot to name itself, which should trip a missing-name error.
---

# Missing Name

No `name` field above. The frontmatter rule should flag the missing required
field while leaving `description` alone.
